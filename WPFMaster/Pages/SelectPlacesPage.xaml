<Page x:Class="WPFMaster.Pages.SelectPlacesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFMaster.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SelectPlacesPage">

    <Grid>
        <StackPanel>
            <!-- Кнопка скрыть занятые -->
            <CheckBox x:Name="ChkHideOccupied"
              Content="Скрыть занятые места"
              Checked="ChkHideOccupied_Changed"
              Unchecked="ChkHideOccupied_Changed"
              Margin="0,0,0,10"/>
            <ItemsControl x:Name="SeatsGrid">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="{Binding RowNumber}" Width="30" VerticalAlignment="Center"/>
                            <ItemsControl ItemsSource="{Binding Seats}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding SeatNumber}"
                                        Width="36" Height="30" Margin="2"
                                        Background="{Binding Background}"
                                        Foreground="{Binding Foreground}"
                                        IsEnabled="{Binding IsEnabled}"
                                        Visibility="{Binding Visibility}"
                                        Command="{Binding ClickCommand}"
                                        ToolTip="{Binding ToolTip}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock x:Name="TxtSelected" Text="Выбрано: 0 мест | Итого: 0 ₽" Margin="0,10,0,0"/>

            <Button x:Name="BtnBook" Content="Оформить билет"
            IsEnabled="False" Margin="0,8,0,0"
            Click="BtnBook_Click"/>
        </StackPanel>
    </Grid>
</Page>
