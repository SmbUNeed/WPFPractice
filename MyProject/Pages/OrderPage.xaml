<Page x:Class="MyProject.Pages.OrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyProject.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrderPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="1" Text="Заказ" VerticalAlignment="Bottom" HorizontalAlignment="Center" FontSize="20"/>

        <StackPanel Grid.Column="1" Grid.Row="1">
            <TextBlock Text="Выберите дату доставки"/>
            <DatePicker x:Name="DateFilter"/>
            <TextBlock Text="Выберите способ оплаты"/>
            <ComboBox x:Name="PaymentMethodCombobox" Height="19">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Margin="10" Content="Подтвердить" Width="100" Height="30" Click="ConfirmButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
