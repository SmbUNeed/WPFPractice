<Page x:Class="MyProject.Pages.RolePages.MasterAppointmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyProject.Pages.RolePages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MasterAppointmentPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" Width="266">
            <TextBlock Text="Дата и время:" FontWeight="Bold"/>
            <TextBlock x:Name="DateTimeText"/>

            <TextBlock Text="ФИО:" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock x:Name="FullnameText"/>

            <TextBlock Text="Телефон:" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock x:Name="PhoneText"/>

            <TextBlock Text="Услуга:" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock x:Name="ServiceText"/>

            <TextBlock Text="Статус:" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock x:Name="StatusText"/>

            <Button x:Name="CompleteButton" Content="Завершить запись"
                Margin="20" Click="CompleteButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
