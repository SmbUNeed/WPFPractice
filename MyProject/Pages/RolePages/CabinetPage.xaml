<Page x:Class="MyProject.Pages.CabinetPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyProject.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CabinetPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.ColumnSpan="2" x:Name="UsernameBox" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>

        <StackPanel Grid.Column="0" Grid.Row="1">
            <TextBlock Text="Записи" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
            <ListBox x:Name="MyAppointmentsListBox">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" CanHorizontallyScroll="False">
                            <TextBlock Width="100" Text="{Binding DateTime}"/>
                            <TextBlock Width="150" Text="{Binding Users1.Fullname}"/>
                            <TextBlock Width="150" Text="{Binding Services.Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <StackPanel Grid.Column="1" Grid.Row="1">
            <TextBlock Text="Заказы" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
            <ListBox x:Name="MyOrdersListBox">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Width="100" Text="{Binding CreatedAt}"/>
                                <ListBox ItemsSource="{Binding OrderProducts}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Products.Name}" Width="100"/>
                                                <TextBlock Text="{Binding Quantity, StringFormat={}{0} шт.}" Width="100"/>
                                            </StackPanel>
                                            
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <TextBlock Width="100" Text="{Binding DeliveryDate}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</Page>
