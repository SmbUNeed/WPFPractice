<Page x:Class="MyProject.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyProject.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage">

    <Grid>
        <ListView Grid.Row="1" Width="380" x:Name="UsersList" Margin="10" d:ItemsSource="{d:SampleData ItemCount=5}" SelectionChanged="UsersList_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="40"/>
                    <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding Fullname}" Width="180"/>
                    <GridViewColumn Header="Роль" DisplayMemberBinding="{Binding Roles.Name}" Width="160"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="+" VerticalAlignment="Top" HorizontalAlignment="Right" Click="NewUserButton_Click"/>
    </Grid>
</Page>
