<Page x:Class="MyProject.Pages.ProductInfoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyProject.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductInfoPage">

    <StackPanel Margin="20">
        <Image Height="300" Source="{Binding Product.ImagePath}"/>
        <TextBlock FontSize="20" Text="{Binding Product.Name}"/>
        <TextBlock Text="{Binding Product.Manufacturers.Name}"/>
        <TextBlock Text="{Binding Product.ProductTypes.Name}"/>
        <TextBlock Text="{Binding Product.Description}"/>
        <TextBlock Text="{Binding Product.Rate, StringFormat={}{0} ★}"/>
        <TextBlock Text="{Binding Product.Price, StringFormat={}{0} руб.}"/>
        <TextBlock Text="{Binding Product.Sale, StringFormat=Скидка: {0}%}"/>

        <StackPanel Orientation="Horizontal">
            <Button Content="В корзину" Command="{Binding AddToCartCommand}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsInCart}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <StackPanel Orientation="Horizontal">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsInCart}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <Button Content="−" Width="30" Command="{Binding DecreaseCommand}"/>
                <TextBlock Text="{Binding Quantity}" Width="30" TextAlignment="Center" VerticalAlignment="Center"/>
                <Button Content="+" Width="30" Command="{Binding IncreaseCommand}"/>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Page>
