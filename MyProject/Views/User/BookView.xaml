<UserControl x:Class="MyProject.Views.BookView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <ScrollViewer>
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Image Grid.Row="0" Grid.RowSpan="3" Grid.Column="0"
                   Source="{Binding Cover, Converter={StaticResource FullPath}}"
                   Height="280" Width="180" Stretch="UniformToFill"
                   VerticalAlignment="Top" Margin="0,0,15,0"/>

            <TextBlock Grid.Row="0" Grid.Column="1"
                       Text="{Binding Title}"
                       FontSize="20" FontWeight="Bold"
                       TextWrapping="Wrap" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="1" Grid.Column="1"
                       Text="{Binding Author}"
                       FontSize="14" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="2" Grid.Column="1"
                       Text="{Binding Genres}"
                       FontSize="13" Foreground="Gray"
                       TextWrapping="Wrap" Margin="0,0,0,8"/>

            <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                        Orientation="Horizontal" Margin="0,5,0,5">
                <Button Content="Читать"
                        Command="{Binding ReadCommand}"
                        Margin="0,0,8,0" Padding="8,4"/>
                <Button Content="Жалоба на книгу"
                        Command="{Binding ComplainBookCommand}"
                        Margin="0,0,8,0" Padding="8,4"/>
                <Button Content="Жалоба на автора"
                        Command="{Binding ComplainAuthorCommand}"
                        Margin="0,0,8,0" Padding="8,4"/>
                <Button Content="Заморозить книгу"
                        Command="{Binding FreezeBookCommand}"
                        Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}"
                        Margin="0,0,8,0" Padding="8,4"/>
            </StackPanel>

            <GroupBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                      Header="Оставить отзыв" Margin="0,10,0,10">
                <StackPanel Margin="5">
                    <TextBlock Text="Оценка (1-10):"/>
                    <TextBox Text="{Binding ReviewRate, UpdateSourceTrigger=PropertyChanged}"
                             Width="60" HorizontalAlignment="Left" Margin="0,2,0,6"/>
                    <TextBlock Text="Текст отзыва:"/>
                    <TextBox Text="{Binding ReviewText, UpdateSourceTrigger=PropertyChanged}"
                             Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,2,0,6"/>
                    <Button Content="Отправить отзыв"
                            Command="{Binding SubmitReviewCommand}"
                            HorizontalAlignment="Left" Padding="8,4"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                      Header="Отзывы" Margin="0,0,0,10">
                <ItemsControl ItemsSource="{Binding Reviews}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="LightGray" BorderThickness="0,0,0,1"
                                    Padding="5" Margin="0,3">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Username}" FontWeight="Bold" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding Rate}" Foreground="Orange"/>
                                        <TextBlock Text="/10" Foreground="Gray"/>
                                        <TextBlock Text="{Binding Date}" Foreground="Gray" Margin="10,0,0,0"/>
                                    </StackPanel>

                                    <TextBlock Grid.Row="1" Text="{Binding Text}"
                                               TextWrapping="Wrap" Margin="0,3,0,3"/>

                                    <StackPanel Grid.Row="2" Orientation="Horizontal">
                                        <Button Content="Пожаловаться на отзыв"
                                                Command="{Binding ComplainCommand}"
                                                Padding="5,2" Margin="0,0,8,0"/>
                                        <Button Content="Заморозить отзыв"
                                                Command="{Binding FreezeCommand}"
                                                Visibility="{Binding CanFreeze, Converter={StaticResource BoolToVis}}"
                                                Padding="5,2"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>
        </Grid>
    </ScrollViewer>
</UserControl>