<UserControl x:Class="MyProject.Views.ReadView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyProject.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding BookTitle}" FontSize="30" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="{Binding ChapterTitle}" FontSize="20" HorizontalAlignment="Center"/>
        <ScrollViewer Grid.Row="2" Width="600" Background="Gray">
            <TextBlock Text="{Binding ChapterText}" TextWrapping="WrapWithOverflow"/>
        </ScrollViewer>
        <Button Grid.Row="3" Command="{Binding PreviousChapter}" 
                Content="Предыдущая глава" 
                Visibility="{Binding PrevAvailable, Converter={StaticResource BoolToVis}}" 
                Width="150" Height="30" HorizontalAlignment="Left" Margin="5"/>
        <Button Grid.Row="3" Command="{Binding NextChapter}" 
                Content="Следующая глава" 
                Visibility="{Binding NextAvailable, Converter={StaticResource BoolToVis}}" 
                Width="150" Height="30" HorizontalAlignment="Right" Margin="5"/>
    </Grid>
</UserControl>
