<UserControl x:Class="MyProject.Views.FrozenAuthorBooksView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock Text="Замороженные книги" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

            <TextBlock Text="Замороженных книг нет" Foreground="Gray"
                       Visibility="{Binding HasNoBooks, Converter={StaticResource BoolToVis}}"/>

            <ItemsControl ItemsSource="{Binding Books}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#FFC107" BorderThickness="1"
                                CornerRadius="4" Margin="0,6" Padding="12">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}"
                                           FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>

                                <StackPanel Visibility="{Binding CanAppeal, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="Причина оспаривания:" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding AppealComment, UpdateSourceTrigger=PropertyChanged}"
                                             Height="50" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,6"/>
                                    <Button Content="Оспорить заморозку"
                                            Command="{Binding AppealCommand}"
                                            HorizontalAlignment="Left" Padding="10,4"/>
                                </StackPanel>

                                <TextBlock Visibility="{Binding HasPendingAppeal, Converter={StaticResource BoolToVis}}"
                                           Text="✓ Заявка на разморозку отправлена"
                                           Foreground="Gray" FontStyle="Italic"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>