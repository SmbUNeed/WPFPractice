<UserControl x:Class="MyProject.Views.RegisterView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyProject.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="1">
            <TextBlock Text="Регистрация" FontSize="20" HorizontalAlignment="Center"/>
            <TextBlock Text="Логин"/>
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Имя"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="e-mail"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Пароль"/>
            <PasswordBox x:Name="PasswordBox"/>
            <TextBlock Text="Повторите пароль"/>
            <PasswordBox x:Name="RepeatPasswordBox"/>
            <Button Content="Уже есть аккаунт?"
                HorizontalAlignment="Left"
                Foreground="Blue"
                Background="Transparent"
                BorderThickness="0"
                Cursor="Hand"
                Command="{Binding ToAuthCommand}"/>
            <Button Content="Зарегистрироваться"
                Width="120"
                Click="RegisterButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
