<UserControl x:Class="MyProject.Views.UsersAdminView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Margin="15">
            <TextBlock Text="Пользователи" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
            <ItemsControl ItemsSource="{Binding Users}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1"
                                CornerRadius="4" Margin="0,5" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="200"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock FontWeight="Bold" Text="{Binding Login}"/>
                                    <TextBlock Text="{Binding Name}"  Foreground="Gray"/>
                                    <TextBlock Text="{Binding Email}" Foreground="Gray"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="10,0">
                                    <TextBlock Text="Роль:" Margin="0,0,0,4"/>
                                    <ComboBox ItemsSource="{Binding AllRoles}"
                                              SelectedItem="{Binding SelectedRole}"
                                              DisplayMemberPath="Name"
                                              Margin="0,0,0,6"/>
                                    <Button Content="Сохранить роль"
                                            Command="{Binding SaveRoleCommand}"
                                            Padding="8,3"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Margin="10,0">
                                    <TextBlock Text="Новый пароль:" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}"
                                             Margin="0,0,0,6"/>
                                    <Button Content="Сменить пароль"
                                            Command="{Binding SavePasswordCommand}"
                                            Padding="8,3"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>