<Window x:Class="BuilderPC.Windows.SaveBuildWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сохранить сборку"
        Width="380" Height="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Название сборки" FontSize="12" Foreground="#555"
                   Margin="0,0,0,4"/>
        <TextBox x:Name="TxtBuildName" Grid.Row="1"
                 Height="32" Padding="8,6" FontSize="13"
                 BorderBrush="#CCC" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="Имя автора" FontSize="12" Foreground="#555"
                   Margin="0,0,0,4"/>
        <TextBox x:Name="TxtAuthor" Grid.Row="3"
                 Height="32" Padding="8,6" FontSize="13"
                 BorderBrush="#CCC"/>

        <Grid Grid.Row="5" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Сохранить"
                    Style="{StaticResource SuccessButton}"
                    FontSize="13" Padding="0,8"
                    Click="BtnSave_Click"/>
            <Button Grid.Column="2" Content="Отмена"
                    FontSize="13" Padding="0,8"
                    Click="BtnCancel_Click"/>
        </Grid>
    </Grid>
</Window>
